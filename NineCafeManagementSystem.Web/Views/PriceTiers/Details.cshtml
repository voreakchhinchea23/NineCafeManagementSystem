@model PriceTierReadOnlyVM

@{
    ViewData["Title"] = "Details";
}

<h1>Details for @Model.PriceRiel</h1>

<div>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.PriceRiel)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.PriceRiel)
        </dd>
    </dl>
</div>
<div>
    @* @if (User.IsInRole(Roles.Admin))
    {
        <a class="btn btn-sm btn-warning" asp-action="Edit" asp-route-id="@Model?.Id">Edit</a>
    } *@
    <a class="btn btn-sm btn-outline-primary" asp-action="Index">Back to List</a>
</div>
